version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./vector_db:/app/vector_db
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
